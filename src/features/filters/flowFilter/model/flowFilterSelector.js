
export const getTeacherId = (state) =>
    state.flowFilterSlice.teacherId

export const getSubjectId = (state) =>
    state.flowFilterSlice.subjectId

export const getIsDelete = (state) =>
    state.flowFilterSlice.isDelete
